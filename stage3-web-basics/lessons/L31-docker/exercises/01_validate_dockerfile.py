"""

from __future__ import annotations

练习 1: Dockerfile 校验器

实现 validate_dockerfile(text: str) -> list[str]
检查：
1. 是否包含 FROM
2. 是否包含 WORKDIR
3. 是否包含 COPY
4. 是否包含 CMD 或 ENTRYPOINT
"""


def validate_dockerfile(text: str) -> list[str]:
    raise NotImplementedError
